<UserControl x:Class="Livestream.Monitor.Views.ImportChannelsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d">
    <Grid FocusManager.FocusedElement="{Binding ElementName=Username}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBox x:Name="Username" Width="200" controls:TextBoxHelper.Watermark="Username goes here..." />
        <Button Grid.Column="1" x:Name="Import" Content="OK" Padding="5"/>
    </Grid>
</UserControl>
