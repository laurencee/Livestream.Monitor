<UserControl x:Class="Livestream.Monitor.Views.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" /> <!-- This is an expanding space to keep the last column on the far right at all times -->
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBox x:Name="StreamName" Margin="2" MinWidth="150" controls:TextBoxHelper.Watermark="channel name..." />
        <Button Grid.Column="1" x:Name="AddStream" Content="+" FontWeight="ExtraBold" Margin="2" ToolTip="Add new channel">
            <Button.ContentTemplate>
                <DataTemplate>
                    <TextBlock Text="{TemplateBinding Content}" Margin="0,-2,0,0"></TextBlock>
                </DataTemplate>
            </Button.ContentTemplate>
        </Button>
        <Button x:Name="ShowImportWindow" Content="Import Channels" Grid.Column="2" Margin="2" ToolTip="Import channels from the provided username" />
        <Separator Grid.Column="3" Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="5,2" />
        <Button x:Name="RefreshChannels" Content="&#x21BB;" FontWeight="ExtraBold" Grid.Column="4" Margin="2" ToolTip="Refresh Channels" />
    </Grid>
</UserControl>
